<%= form_with(model: artefato, local: true) do |form| %>
  <% if artefato.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(artefato.errors.count, "erro(s)") %> - Artefato não pôde ser salvo!</h2>

      <ul>
      <% artefato.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card-body">
    <%= form.hidden_field :tema_curso_id %>
  
    <div class="form-group row">
      <label for="inputNome" class="col-md-3 col-form-label">Nome do artefato*</label>
      <div class="col-md-9">
        <%= form.text_field :nome, autofocus: true, class: "form-control", required: true %>
      </div>
    </div>

    <div class="form-group row">
      <label for="inputNome" class="col-md-3 col-form-label">Pontos requeridos*</label>
      <div class="col-md-2">
        <%= form.number_field :pontos_requeridos, autofocus: true, class: "form-control", required: true %>
      </div>
    </div>

    <div class="form-group row">
      <label for="inputNome" class="col-md-3 col-form-label">Imagem*</label>
      <div class="col-md-9">
        <%= form.file_field :imagem, as: :file %>
      </div>
    </div>

    <div class="form-group row">
      <label class="form-check-label col-md-3" for="gridCheck1">Ativo</label>
      <div class="col-md-2 ">
        <div class="form-check">
          <%= form.check_box :ativo, class: "form-check-input" %> Sim
        </div>
      </div>
    </div>

  </div>

  <div class="card-footer text-muted">
    <%= form.submit "Salvar", class: "btn btn-md btn-primary" %>
    <%= link_to 'Voltar', :back, class: "btn btn-md btn-secondary" %>
  </div>

<% end %>