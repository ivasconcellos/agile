<%= form_with(model: sala_chat, local: true) do |form| %>
  <% if sala_chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sala_chat.errors.count, "error") %> prohibited this sala_chat from being saved:</h2>

      <ul>
      <% sala_chat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
