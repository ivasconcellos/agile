<%= form_with(model: email, local: true) do |form| %>
  <% if email.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <h2><%= pluralize(email.errors.count, "erro(s)") %> - E-mail não pôde ser enviado!</h2>

      <ul>
      <% email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="card-body">
    <%= form.hidden_field :destinatario_id %>

    <div class="form-group row">
      <label for="inputNome" class="col-md-2 col-form-label">Assunto*</label>
      <div class="col-md-10">
        <%= form.text_field :assunto, autofocus: true, class: "form-control" %>
      </div>
    </div>

    <div class="form-group row">
      <div class="col-md-12">
        <%= form.cktext_area :mensagem, class: "form-control" %>
      </div>
    </div>

  <div class="card-footer text-muted">
    <%= form.submit "Enviar", class: "btn btn-md btn-primary" %>
    <%= link_to 'Voltar', emails_path, class: "btn btn-md btn-secondary" %>
  </div>
<% end %>
