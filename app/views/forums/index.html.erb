
<div class="card">
  <div class="card-header">Forums do Curso</div>

  <div class="table-responsive">
    <table class="table table-striped table-sm">
      <!-- <thead>
        <tr>
          <th class="text-center">Titulo</th>
        </tr>
        <tr>
          <th class="text-">Descricao</th>
        </tr>
      </thead> -->
      <tbody>
        <br>
        <% @forums.each do |forum| %>
        <table>
        <tr>

        <h3 class="text-center"><%= forum.titulo %></h3>
        <p class="text-left"><%= forum.descricao %></p>
      </tr>
      <tr>
        <td style="padding: 10px;"><%= link_to 'Show', forum %></td>
        <td style="padding: 10px;"><%= link_to 'Edit', edit_forum_path(forum) %></td>
        <td style="padding: 10px;"><%= link_to 'Destroy', forum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td> <div class="text-center">
            <%= link_to 'Comentar', {controller: 'comentarios', action: 'new', forum_id: forum.id}, class: "btn btn=md btn-primary" %>
        </div></td>
      </tr>
    </table>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="text-center">
    <%= link_to 'New forum', {controller: 'forums', action: 'new', curso_id: @current_usuario.curso_atual_id}, class: "btn btn-md btn-primary" %>
  </div>



  <br>


</div>
